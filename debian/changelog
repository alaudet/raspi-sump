raspisump (2.0~a28) unstable; urgency=medium

  * Phase 5c Flask admin: backup/export downloads zip of database and
    raspisump.conf; credentials excluded; Administration nav dropdown added

 -- Al Audet <alaudet@linuxnorth.org>  Fri, 13 Mar 2026 00:00:00 +0000

raspisump (2.0~a27) unstable; urgency=medium

  * Phase 5b Flask admin: config viewer displays raspisump.conf sections and
    values read-only; credentials.conf never read

 -- Al Audet <alaudet@linuxnorth.org>  Fri, 13 Mar 2026 00:00:00 +0000

raspisump (2.0~a26) unstable; urgency=medium

  * Phase 5a Flask admin: service status panel showing ActiveState, SubState,
    start timestamp, and PID for raspisump, rsumpweb, and rsumpwebchart services

 -- Al Audet <alaudet@linuxnorth.org>  Fri, 13 Mar 2026 00:00:00 +0000

raspisump (2.0~a25) unstable; urgency=medium

  * Phase 4 Flask web interface: password-protected admin stub with
    login/logout; admin password stored in credentials.conf [web] section

 -- Al Audet <alaudet@linuxnorth.org>  Fri, 13 Mar 2026 00:00:00 +0000

raspisump (2.0~a24) unstable; urgency=medium

  * Fix postinst: drop -quiet flag from openssl (not available on all
    platforms); try -noenc before -nodes for OpenSSL 3.x compatibility

 -- Al Audet <alaudet@linuxnorth.org>  Fri, 13 Mar 2026 00:00:00 +0000

raspisump (2.0~a23) unstable; urgency=medium

  * Phase 3 Flask web interface: historical readings page with date picker,
    on-demand chart generation, stat cards for any past date

 -- Al Audet <alaudet@linuxnorth.org>  Fri, 13 Mar 2026 00:00:00 +0000

raspisump (2.0~a22) unstable; urgency=medium

  * Phase 2 Flask web interface: homepage stat cards showing min/max water
    level, reading count, and last reading with timestamp

 -- Al Audet <alaudet@linuxnorth.org>  Fri, 13 Mar 2026 00:00:00 +0000

raspisump (2.0~a21) unstable; urgency=medium

  * Phase 1 Flask web interface: homepage with today's chart, mobile-first
    CSS, HTTPS via self-signed cert, nginx proxy to Flask Unix socket,
    rsumpweb systemd service

 -- Al Audet <alaudet@linuxnorth.org>  Fri, 13 Mar 2026 00:00:00 +0000

raspisump (2.0~a20) unstable; urgency=medium

  * Fix raspisump.service: set StateDirectoryMode=0771 to match postinst,
    preventing systemd from resetting /var/lib/raspi-sump to 0770 on every
    service start and clobbering nginx traversal permissions

 -- Al Audet <alaudet@linuxnorth.org>  Thu, 12 Mar 2026 00:00:00 +0000

raspisump (2.0~a19) unstable; urgency=medium

  * Fix postinst: set /var/lib/raspi-sump to 0771 so nginx can traverse
    into charts/ directory without group membership

 -- Al Audet <alaudet@linuxnorth.org>  Thu, 12 Mar 2026 00:00:00 +0000

raspisump (2.0~a18) unstable; urgency=medium

  * Add rsumplog CLI command for querying readings from the SQLite database
  * Supports --today (default), --date YYYY-MM-DD, and --last N options
  * Add query_readings() to log.py for testable database queries
  * Add tests/test_rsumplog.py with 6 tests covering all query modes

 -- Al Audet <alaudet@linuxnorth.org>  Thu, 12 Mar 2026 00:00:00 +0000

raspisump (2.0~a17) unstable; urgency=medium

  * Fix raspisump.service: add StateDirectoryMode=0770 and LogsDirectoryMode=0770
    so systemd does not reset /var/lib/raspi-sump to 0750 on every service restart

 -- Al Audet <alaudet@linuxnorth.org>  Wed, 11 Mar 2026 00:00:00 +0000

raspisump (2.0~a16) unstable; urgency=medium

  * Fix postinst: correct raspisump.db ownership/permissions on upgrade (0664 raspisump:raspisump)
  * Fix .gitignore: add debian/.debhelper/ to prevent build artifacts from being tracked

 -- Al Audet <alaudet@linuxnorth.org>  Wed, 11 Mar 2026 00:00:00 +0000

raspisump (2.0~a15) unstable; urgency=medium

  * First working SQLite release — data confirmed writing and querying on device
  * Fix db file permissions: set umask before sqlite3.connect() so file is created 0664
  * Fix /var/lib/raspi-sump/ permissions: add setgid bit so WAL files inherit raspisump group
  * Add sqlite3 CLI as Recommends in debian/control
  * Add pythonpath = ["."] to pytest config so tests use local source over installed package

 -- Al Audet <alaudet@linuxnorth.org>  Tue, 11 Mar 2026 00:00:00 +0000

raspisump (2.0~a14) unstable; urgency=medium

  * Complete SQLite migration — log.py, todaychart.py, webchart.py, reading.py
  * Store unit as display label (cm/inches) rather than config key (metric/imperial)
  * Rename water_cm column to water_depth for unit-agnostic schema

 -- Al Audet <alaudet@linuxnorth.org>  Wed, 11 Mar 2026 00:00:00 +0000

raspisump (2.0~a13) unstable; urgency=medium

  * Begin SQLite migration branch — revert to alpha status
  * CSV-based data store will be replaced with SQLite in upcoming releases

 -- Al Audet <alaudet@linuxnorth.org>  Wed, 11 Mar 2026 00:00:00 +0000

raspisump (2.0~rc3) unstable; urgency=medium

  * Change license from MIT to Apache 2.0
  * Update LICENSE, pyproject.toml, debian/copyright, source headers, README, CONTRIBUTING

 -- Al Audet <alaudet@linuxnorth.org>  Wed, 11 Mar 2026 00:00:00 +0000

raspisump (2.0~rc2) unstable; urgency=medium

  * First pre-release of 2.0
  * Add nginx site config; disable default site on install to avoid port 80 conflict
  * Add web footer showing installed package version (generated by postinst)
  * Ship css/inc/VERSION placeholder; postinst overwrites with real package version
  * On upgrade, restart services that were already running; fresh installs still require manual start
  * Fix upgrade restart: use is-enabled instead of is-active (prerm stops services before postinst runs)

 -- Al Audet <alaudet@linuxnorth.org>  Tue, 10 Mar 2026 00:00:00 +0000

raspisump (2.0~a12) unstable; urgency=medium

  * Make charts dir world-readable (0775) so nginx can serve without group membership
  * Remove www-data from raspisump group — eliminates nginx access to credentials.conf
  * Update webchart: create year/month dirs with mode 0775 instead of 0770

 -- Al Audet <alaudet@linuxnorth.org>  Mon, 10 Mar 2026 00:00:00 +0000

raspisump (2.0~a11) unstable; urgency=medium

  * Add nginx site config for web assets and charts serving
  * postinst: add www-data to raspisump group; create nginx symlink on install
  * postrm: remove nginx symlink on remove/purge

 -- Al Audet <alaudet@linuxnorth.org>  Mon, 10 Mar 2026 00:00:00 +0000

raspisump (2.0~a10) unstable; urgency=medium

  * Fix webchart: use umask 0002 when creating chart dirs and PNG so group members can read/write

 -- Al Audet <alaudet@linuxnorth.org>  Mon, 10 Mar 2026 00:00:00 +0000

raspisump (2.0~a9) unstable; urgency=medium

  * Fix todaychart: remove .decode("utf8") from numpy converter (NumPy >= 1.23 passes str not bytes)

 -- Al Audet <alaudet@linuxnorth.org>  Mon, 10 Mar 2026 00:00:00 +0000

raspisump (2.0~a8) unstable; urgency=medium

  * Install configs to /usr/share/raspi-sump/examples/ — never overwrite on upgrade
  * postinst copies examples to /etc/raspi-sump/ on first install only
  * Show upgrade message pointing to updated example configs

 -- Al Audet <alaudet@linuxnorth.org>  Mon, 10 Mar 2026 00:00:00 +0000

raspisump (2.0~a7) unstable; urgency=medium

  * Do not auto-start service on install; display config instructions instead

 -- Al Audet <alaudet@linuxnorth.org>  Mon, 10 Mar 2026 00:00:00 +0000

raspisump (2.0~a6) unstable; urgency=medium

  * Set setgid bit on shared dirs so files inherit group raspisump
  * Use umask 0002 when creating log/csv files so they are group-writable

 -- Al Audet <alaudet@linuxnorth.org>  Mon, 10 Mar 2026 00:00:00 +0000

raspisump (2.0~a5) unstable; urgency=medium

  * Add UMask=0002 to service files so created files are group-writable
  * Add /var/lib/raspi-sump/support to postinst directory creation

 -- Al Audet <alaudet@linuxnorth.org>  Mon, 10 Mar 2026 00:00:00 +0000

raspisump (2.0~a4) unstable; urgency=medium

  * Remove Group=gpio from service files; raspisump user retains gpio as supplementary group

 -- Al Audet <alaudet@linuxnorth.org>  Mon, 10 Mar 2026 00:00:00 +0000

raspisump (2.0~a3) unstable; urgency=medium

  * Add installing user to raspisump group during postinst
  * Create /var/lib/raspi-sump/csv, charts and /var/log/raspi-sump in postinst

 -- Al Audet <alaudet@linuxnorth.org>  Mon, 10 Mar 2026 00:00:00 +0000

raspisump (2.0~a2) unstable; urgency=medium

  * Split credentials into /etc/raspi-sump/credentials.conf (mode 0640)
  * Fixed systemd service startup: added WorkingDirectory=/var/lib/raspi-sump
  * Mocked GPIO in tests so package build does not require hardware

 -- Al Audet <alaudet@linuxnorth.org>  Mon, 09 Mar 2026 00:00:00 +0000

raspisump (2.0~a1) unstable; urgency=medium

  * Version 2.0 alpha release
  * Migrated to pyproject.toml (PEP 517/518)
  * Migrated from hcsr04sensor to pinsource (lgpio, Raspberry Pi 5 compatible)
  * Migrated all paths to Linux FHS conventions
  * Removed os.getlogin() calls for systemd compatibility
  * Added console script entry points via cli.py

 -- Al Audet <alaudet@linuxnorth.org>  Mon, 09 Mar 2026 00:00:00 +0000
